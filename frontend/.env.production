# Used automatically by "npm run build" (and by Vercel's build step).
# Relative path works because frontend + backend are deployed as ONE Vercel
# project — /api/* is routed to the backend serverless function (see /vercel.json).
REACT_APP_BASE_URL=/api
